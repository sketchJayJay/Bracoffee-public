:root{
  --bg:#f5f2ed;
  --surface:#fffdfa;
  --surface-2:#f0ebe4;
  --ink:#26211d;
  --muted:#7c746c;
  --line:#e7dfd5;
  --brown:#8a6035;
  --brown-2:#6f4c2c;
  --gold:#d9c197;
  --gold-2:#efe3cd;
  --green:#2f7a56;
  --green-bg:#e9f5ef;
  --amber:#9a6a12;
  --amber-bg:#fff5df;
  --red:#a5483f;
  --red-bg:#fcebe8;
  --shadow:0 14px 40px rgba(71,48,27,.08);
  --radius:20px;
  --sidebar:248px;
}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:var(--bg);color:var(--ink)}
button,input,select,textarea{font:inherit}
button{cursor:pointer}
button:disabled{opacity:.45;cursor:not-allowed}
.app-shell{min-height:100vh;display:grid;grid-template-columns:var(--sidebar) 1fr}
.sidebar{position:sticky;top:0;height:100vh;background:#221e1b;color:#fff;padding:22px 16px;display:flex;flex-direction:column;z-index:10}
.brand-wrap{height:74px;display:flex;align-items:center;padding:0 8px 18px;border-bottom:1px solid rgba(255,255,255,.08)}
.brand-logo{width:100%;max-width:186px;height:auto;filter:none}
.nav{display:flex;flex-direction:column;gap:6px;padding-top:20px}
.nav-item{border:0;background:transparent;color:#cfc7bf;display:flex;align-items:center;gap:12px;padding:12px 14px;border-radius:13px;text-align:left;font-weight:700;transition:.18s ease}
.nav-item:hover{background:rgba(255,255,255,.06);color:#fff}
.nav-item.active{background:#f0e1c8;color:#4d3824}
.nav-icon{width:27px;height:27px;border-radius:8px;display:grid;place-items:center;background:rgba(255,255,255,.06);font-size:14px;font-weight:900}
.nav-item.active .nav-icon{background:rgba(138,96,53,.13)}
.sidebar-footer{margin-top:auto;padding-top:18px;border-top:1px solid rgba(255,255,255,.08)}
.version{font-size:11px;color:#8f8882;margin-top:12px;text-align:center}
.main{min-width:0;padding:28px 36px 52px}
.topbar{display:flex;justify-content:space-between;gap:20px;align-items:center;margin-bottom:26px}
.topbar h1,.section-intro h2{margin:3px 0 0;font-size:28px;letter-spacing:-.03em}
.topbar-actions{display:flex;gap:10px;align-items:center}
.eyebrow{margin:0;color:var(--brown);font-size:11px;font-weight:900;letter-spacing:.18em;text-transform:uppercase}
.eyebrow.light{color:#ead6b8}
.page{display:none;animation:fadeIn .18s ease}
.page.active{display:block}
@keyframes fadeIn{from{opacity:.5;transform:translateY(4px)}to{opacity:1;transform:none}}
.primary-btn,.ghost-btn,.hero-action,.text-btn,.icon-btn{border:0;border-radius:12px;font-weight:800;transition:.16s ease}
.primary-btn{background:var(--brown);color:#fff;padding:11px 16px;box-shadow:0 7px 20px rgba(138,96,53,.18)}
.primary-btn:hover{background:var(--brown-2);transform:translateY(-1px)}
.primary-btn.large{padding:13px 20px;font-size:15px}
.ghost-btn{background:var(--surface);border:1px solid var(--line);color:var(--ink);padding:10px 14px}
.ghost-btn:hover{background:var(--surface-2)}
.ghost-btn.full{width:100%;background:rgba(255,255,255,.06);border-color:rgba(255,255,255,.08);color:#d9d1ca}
.text-btn{background:transparent;color:var(--brown);padding:6px 0}
.icon-btn{width:38px;height:38px;background:var(--surface-2);font-size:22px;color:var(--ink)}
.hero-card{background:linear-gradient(135deg,#2a211b 0%,#4b3524 56%,#77512f 100%);color:#fff;border-radius:26px;padding:30px;display:flex;justify-content:space-between;gap:24px;align-items:end;box-shadow:var(--shadow);overflow:hidden;position:relative}
.hero-card:after{content:"";position:absolute;width:310px;height:310px;border-radius:50%;right:-90px;top:-150px;background:rgba(239,227,205,.08)}
.hero-card h2{font-size:30px;letter-spacing:-.035em;margin:7px 0 8px;max-width:620px}
.hero-card p{color:#d8cec4;max-width:690px;margin:0;line-height:1.55}
.hero-action{position:relative;z-index:1;background:#f2e3ca;color:#4d3824;padding:13px 16px;white-space:nowrap}
.hero-action span{margin-left:8px}
.stats-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:14px;margin:18px 0}
.stats-grid.compact{grid-template-columns:repeat(3,minmax(0,1fr))}
.stat-card{background:var(--surface);border:1px solid var(--line);border-radius:18px;padding:18px;box-shadow:0 6px 20px rgba(63,42,24,.04)}
.stat-label{font-size:12px;color:var(--muted);font-weight:800;text-transform:uppercase;letter-spacing:.06em}
.stat-value{font-size:24px;font-weight:900;margin-top:6px;letter-spacing:-.04em}
.stat-help{font-size:12px;color:var(--muted);margin-top:5px}
.two-col{display:grid;grid-template-columns:1.25fr .75fr;gap:18px}
.panel,.table-card{background:var(--surface);border:1px solid var(--line);border-radius:var(--radius);box-shadow:0 8px 28px rgba(63,42,24,.04)}
.panel{padding:20px}
.panel-head{display:flex;align-items:center;justify-content:space-between;gap:14px;margin-bottom:14px}
.panel-head h3,.form-card h3,.modal-head h3{margin:3px 0 0;font-size:18px}
.list-stack{display:flex;flex-direction:column;gap:8px}
.list-row{display:flex;justify-content:space-between;gap:14px;align-items:center;padding:12px;border-radius:14px;background:#faf7f2;border:1px solid #eee7de}
.list-row strong{display:block}
.list-row small{color:var(--muted)}
.amount{font-weight:900;text-align:right}
.stock-summary{display:flex;flex-direction:column;gap:12px}
.stock-line{display:grid;grid-template-columns:1fr auto;gap:12px;align-items:center}
.stock-bar{height:7px;border-radius:999px;background:#ece5db;overflow:hidden;margin-top:6px}
.stock-bar > i{display:block;height:100%;background:linear-gradient(90deg,var(--gold),var(--brown));border-radius:999px}
.empty-state{padding:34px 18px;text-align:center;border:1px dashed #d9cec0;border-radius:16px;color:var(--muted);background:#fbf9f6}
.empty-state strong{display:block;color:var(--ink);margin-bottom:5px}
.section-intro{display:flex;align-items:end;justify-content:space-between;gap:20px;margin-bottom:18px}
.section-intro p:last-child{margin:7px 0 0;color:var(--muted)}
.step-pill{background:var(--gold-2);color:#624727;border-radius:999px;padding:8px 12px;font-size:12px;font-weight:900}
.form-layout{display:flex;flex-direction:column;gap:14px}
.form-card{background:var(--surface);border:1px solid var(--line);border-radius:20px;padding:20px}
.form-card-title{display:flex;gap:12px;align-items:center;margin-bottom:18px}
.form-card-title>span{width:34px;height:34px;border-radius:11px;display:grid;place-items:center;background:var(--gold-2);color:var(--brown);font-weight:900}
.form-card-title p{margin:2px 0 0;color:var(--muted);font-size:13px}
.form-grid{display:grid;gap:14px}
.form-grid.cols-2{grid-template-columns:repeat(2,minmax(0,1fr))}
.form-grid.cols-3{grid-template-columns:repeat(3,minmax(0,1fr))}
.span-2{grid-column:span 2}.span-3{grid-column:span 3}
.field{display:flex;flex-direction:column;gap:7px;font-size:13px;font-weight:800;color:#544c45}
.field input,.field select,.field textarea,.toolbar select{width:100%;border:1px solid #ddd4ca;background:#fff;border-radius:12px;padding:11px 12px;color:var(--ink);outline:none;transition:.15s ease}
.field textarea{resize:vertical;min-height:84px}
.field input:focus,.field select:focus,.field textarea:focus,.search-box:focus-within,.toolbar select:focus{border-color:#ba9369;box-shadow:0 0 0 3px rgba(186,147,105,.13)}
.money-input{display:flex;border:1px solid #ddd4ca;border-radius:12px;background:#fff;overflow:hidden}
.money-input:focus-within{border-color:#ba9369;box-shadow:0 0 0 3px rgba(186,147,105,.13)}
.money-input span{display:flex;align-items:center;padding:0 11px;background:#f6f0e8;color:#7b6a5a;font-weight:900;border-right:1px solid #e7ded4}
.money-input input{border:0!important;border-radius:0!important;box-shadow:none!important}
.computed-value{min-height:43px;display:flex;align-items:center;padding:0 12px;border-radius:12px;background:#f3eee7;border:1px solid #e2d8cd;font-size:17px;font-weight:900;color:#5b432c}
.sticky-form-actions{position:sticky;bottom:14px;z-index:5;background:rgba(255,253,250,.95);border:1px solid var(--line);box-shadow:0 14px 34px rgba(59,39,22,.14);backdrop-filter:blur(12px);border-radius:18px;padding:11px;display:flex;align-items:center;gap:12px}
.action-copy{margin-left:auto;color:var(--muted);font-size:12px}.action-copy strong{color:var(--ink)}
.toolbar{display:flex;gap:10px;align-items:center;margin-bottom:14px}
.search-box{flex:1;display:flex;align-items:center;gap:8px;background:#fff;border:1px solid #ddd4ca;border-radius:12px;padding:0 12px;color:#8b7e72}
.search-box input{width:100%;border:0;outline:none;background:transparent;padding:11px 0;color:var(--ink)}
.search-box.small{max-width:320px}
.toolbar select{width:auto;min-width:145px}
.cards-list{display:flex;flex-direction:column;gap:10px}
.data-card{background:var(--surface);border:1px solid var(--line);border-radius:17px;padding:16px;display:grid;grid-template-columns:minmax(170px,1.2fr) repeat(4,minmax(100px,.7fr)) auto;gap:14px;align-items:center}
.data-card .main-info strong{font-size:15px}.data-card .main-info small{display:block;margin-top:4px;color:var(--muted)}
.data-meta label{display:block;font-size:10px;text-transform:uppercase;letter-spacing:.08em;color:#968a7e;font-weight:900}.data-meta strong{display:block;margin-top:3px}
.card-actions{display:flex;gap:6px;justify-content:flex-end;flex-wrap:wrap}
.mini-btn{border:1px solid #ddd3c8;background:#fff;border-radius:10px;padding:8px 10px;font-size:12px;font-weight:900;color:#5b5046}
.mini-btn:hover{background:#f4eee7}
.mini-btn.primary{background:var(--brown);color:#fff;border-color:var(--brown)}
.badge{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:6px 9px;font-size:11px;font-weight:900;white-space:nowrap}
.badge.green{color:var(--green);background:var(--green-bg)}.badge.amber{color:var(--amber);background:var(--amber-bg)}.badge.red{color:var(--red);background:var(--red-bg)}.badge.gray{color:#6e665f;background:#efebe7}
.table-card{overflow:hidden}.table-wrap{overflow-x:auto}table{border-collapse:collapse;width:100%;min-width:850px}th,td{text-align:left;padding:14px 16px;border-bottom:1px solid #eee6dc;font-size:13px}th{font-size:10px;text-transform:uppercase;letter-spacing:.09em;color:#8f8276;background:#faf7f3}tbody tr:hover{background:#fcfaf7}.table-link{border:0;background:transparent;color:var(--brown);font-weight:900}.mobile-cards{display:none}
.modal{width:min(650px,calc(100vw - 28px));max-height:88vh;border:0;border-radius:22px;padding:0;background:var(--surface);box-shadow:0 25px 80px rgba(35,27,21,.28);overflow:auto}
.modal.wide{width:min(820px,calc(100vw - 28px))}
.modal::backdrop{background:rgba(26,22,18,.55);backdrop-filter:blur(3px)}
.modal>form,.modal>div:not(.modal-head):not(.modal-actions){ }
.modal-head{display:flex;justify-content:space-between;gap:12px;align-items:center;padding:20px 20px 14px;border-bottom:1px solid var(--line);position:sticky;top:0;background:rgba(255,253,250,.97);backdrop-filter:blur(10px);z-index:2}
.modal form>.form-grid,.modal>#purchaseDetailBody,.modal form>.lot-highlight{padding:18px 20px}
.modal-actions{display:flex;justify-content:flex-end;gap:9px;padding:14px 20px 20px;border-top:1px solid var(--line)}
.modal-actions.wrap{flex-wrap:wrap}
.detail-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}
.detail-box{background:#f8f4ee;border:1px solid #e9e0d6;border-radius:14px;padding:13px}.detail-box label{font-size:10px;text-transform:uppercase;color:#948679;font-weight:900;letter-spacing:.08em}.detail-box strong{display:block;margin-top:4px}.detail-box.span-2{grid-column:span 2}
.detail-section{margin-top:16px}.detail-section h4{margin:0 0 10px}.timeline{display:flex;flex-direction:column;gap:8px}.timeline-row{display:flex;justify-content:space-between;gap:15px;padding:10px 12px;border-radius:12px;background:#faf7f2}.timeline-row small{color:var(--muted)}
.lot-highlight{background:#f7efe4;margin:18px 20px 0;border-radius:14px;color:#5f432a}
.settings-tools{margin:18px 20px 0;padding:14px;border:1px solid var(--line);border-radius:15px;display:flex;align-items:center;justify-content:space-between;gap:16px}.settings-tools p{margin:3px 0 0;color:var(--muted);font-size:12px}.inline-actions{display:flex;gap:8px}
.toast-stack{position:fixed;right:20px;bottom:20px;z-index:1000;display:flex;flex-direction:column;gap:8px;pointer-events:none}
.toast{background:#28231f;color:#fff;border-radius:13px;padding:12px 14px;box-shadow:0 16px 38px rgba(0,0,0,.18);min-width:250px;animation:toastIn .2s ease}.toast.success{border-left:4px solid #62b88c}.toast.error{border-left:4px solid #d76d62}.toast small{display:block;color:#cfc7bf;margin-top:3px}@keyframes toastIn{from{opacity:0;transform:translateY(7px)}to{opacity:1;transform:none}}
.mobile-nav{display:none}
@media(max-width:1120px){.stats-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.two-col{grid-template-columns:1fr}.data-card{grid-template-columns:1.2fr repeat(2,.7fr) auto}.data-card .hide-mid{display:none}}
@media(max-width:820px){
  body{padding-bottom:76px}.app-shell{display:block}.sidebar{display:none}.main{padding:20px 14px 30px}.topbar{margin-bottom:18px}.topbar h1{font-size:23px}.topbar-actions .ghost-btn,.hide-mobile{display:none!important}.primary-btn{padding:10px 13px}.hero-card{padding:22px;display:block}.hero-card h2{font-size:25px}.hero-action{margin-top:18px}.stats-grid,.stats-grid.compact{grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.stat-card{padding:14px}.stat-value{font-size:21px}.section-intro{align-items:flex-start}.row-mobile-stack{align-items:stretch}.form-card{padding:15px}.form-grid.cols-3{grid-template-columns:repeat(2,minmax(0,1fr))}.form-grid.cols-3 .span-3{grid-column:span 2}.sticky-form-actions{bottom:84px}.toolbar{align-items:stretch}.toolbar select{min-width:120px}.data-card{grid-template-columns:1fr 1fr}.data-card .main-info{grid-column:span 2}.data-card .card-actions{grid-column:span 2;justify-content:flex-start}.table-wrap{display:none}.mobile-cards{display:flex;flex-direction:column;gap:9px;padding:10px}.purchase-mobile-card{border:1px solid var(--line);border-radius:15px;padding:13px;background:#fff}.purchase-mobile-top{display:flex;justify-content:space-between;gap:10px}.purchase-mobile-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px;margin:12px 0}.purchase-mobile-grid small{display:block;color:var(--muted)}.mobile-nav{position:fixed;z-index:50;left:8px;right:8px;bottom:8px;height:66px;background:rgba(37,31,27,.96);backdrop-filter:blur(14px);border-radius:18px;display:grid;grid-template-columns:repeat(5,1fr);padding:7px;box-shadow:0 18px 40px rgba(0,0,0,.2)}.mobile-nav-item{border:0;background:transparent;color:#9e958d;border-radius:12px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:2px}.mobile-nav-item span{font-size:17px;font-weight:900}.mobile-nav-item small{font-size:9px;font-weight:800}.mobile-nav-item.active{background:#ead9bd;color:#513b26}.toast-stack{left:12px;right:12px;bottom:87px}.toast{min-width:0}.settings-tools{flex-direction:column;align-items:stretch}
}
@media(max-width:560px){.topbar-actions #quickBuyBtn{display:none}.hero-card h2{font-size:23px}.stats-grid,.stats-grid.compact{grid-template-columns:1fr}.form-grid.cols-2,.form-grid.cols-3{grid-template-columns:1fr}.span-2,.span-3,.form-grid.cols-3 .span-3{grid-column:span 1}.row-mobile-stack{flex-direction:column}.toolbar{flex-direction:column}.toolbar select{width:100%}.search-box.small{max-width:none}.data-card{grid-template-columns:1fr}.data-card .main-info,.data-card .card-actions{grid-column:span 1}.detail-grid{grid-template-columns:1fr}.detail-box.span-2{grid-column:span 1}.sticky-form-actions{justify-content:space-between}.sticky-form-actions .ghost-btn{padding-inline:11px}.modal-actions{justify-content:stretch}.modal-actions button{flex:1}.inline-actions{flex-wrap:wrap}.inline-actions button{flex:1}}
@media print{body{background:#fff}.sidebar,.topbar,.mobile-nav,.sticky-form-actions{display:none!important}.main{padding:0}.page{display:block!important}}
